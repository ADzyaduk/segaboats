# 🌐 Настройка DNS серверов Beget

## Информация от Beget

Для работы домена через Beget нужно указать DNS серверы:
- `ns1.beget.com`
- `ns2.beget.com`
- `ns1.beget.pro`
- `ns2.beget.pro`

## Варианты настройки

### Вариант 1: Использовать поддомен Beget (уже работает)

Если вы используете `klernesokof.beget.app` - это **поддомен Beget**, DNS серверы уже настроены автоматически. 

**Проблема:** Поддомены Beget обычно указывают на IP Beget, а не на ваш сервер.

**Решение:** Нужно настроить A-запись в панели Beget для поддомена.

### Вариант 2: Использовать свой домен

Если у вас есть свой домен (например, `yourdomain.com`):

1. **В панели регистратора домена** (где вы купили домен):
   - Найдите настройки DNS серверов
   - Измените на:
     - `ns1.beget.com`
     - `ns2.beget.com`
     - `ns1.beget.pro`
     - `ns2.beget.pro`

2. **В панели Beget:**
   - Добавьте домен в управление
   - Создайте поддомен или используйте корневой домен
   - Настройте A-запись: `@` → `155.212.189.214`

3. **Подождите 5-10 минут** для распространения DNS

## Для текущей задачи (klernesokof.beget.app)

Если вы используете поддомен `klernesokof.beget.app`:

1. **Зайдите в панель Beget**
2. **Найдите управление поддоменами** или **DNS записи**
3. **Добавьте/измените A-запись:**
   - **Имя:** `klernesokof` (или `@` для корневого)
   - **Тип:** `A`
   - **Значение:** `155.212.189.214`
   - **TTL:** `3600`

4. **Проверьте DNS:**
   ```bash
   nslookup klernesokof.beget.app
   # Должен вернуться: 155.212.189.214
   ```

## Альтернатива: Использовать ngrok (быстрое решение)

Если настройка DNS в Beget занимает время, используйте ngrok для тестирования:

```bash
# На сервере
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar xvzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin/

# Зарегистрируйтесь на ngrok.com и получите токен
ngrok config add-authtoken YOUR_NGROK_TOKEN

# Запустите туннель
ngrok http 80

# Скопируйте HTTPS URL (например: https://abc123.ngrok-free.app)
# Используйте его для webhook:
curl -X POST "https://api.telegram.org/bot8533883256:AAGGtdzBkMiPdswF49zAiYesz3eaqVyafkU/setWebhook" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://abc123.ngrok-free.app/api/telegram/webhook",
    "secret_token": "test-secret-123",
    "allowed_updates": ["message", "callback_query"]
  }'
```

## Проверка

После настройки DNS:

```bash
# Проверка DNS
nslookup klernesokof.beget.app

# Проверка доступности
curl -I https://klernesokof.beget.app/health

# Проверка webhook
curl "https://api.telegram.org/bot8533883256:AAGGtdzBkMiPdswF49zAiYesz3eaqVyafkU/getWebhookInfo"
```

## Важно

- ⏱️ DNS изменения могут занять до 24 часов (обычно 5-10 минут)
- 🔒 Убедитесь, что порты 80 и 443 открыты на сервере
- 📝 Если используете свой домен, DNS серверы нужно менять у регистратора домена
