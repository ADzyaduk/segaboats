# System Nginx configuration for v-more.ru (primary) and v-more.store (redirect)
# This file should be placed in /etc/nginx/sites-available/boats2026
# and symlinked to /etc/nginx/sites-enabled/

# Rate limiting zones
limit_req_zone $binary_remote_addr zone=api_zone:10m rate=10r/s;

# Redirect v-more.store to v-more.ru
server {
    listen 80;
    server_name v-more.store www.v-more.store;
    return 301 https://v-more.ru$request_uri;
}

server {
    listen 443 ssl;
    server_name v-more.store www.v-more.store;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/v-more.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/v-more.ru/privkey.pem;

    return 301 https://v-more.ru$request_uri;
}

# Main server (v-more.ru)
server {
    listen 80;
    server_name v-more.ru www.v-more.ru;

    # Let's Encrypt verification
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all HTTP to HTTPS
    location / {
        return 301 https://v-more.ru$request_uri;
    }
}

# Redirect www.v-more.ru to v-more.ru
server {
    listen 443 ssl;
    server_name www.v-more.ru;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/v-more.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/v-more.ru/privkey.pem;

    return 301 https://v-more.ru$request_uri;
}

# Primary server (v-more.ru)
server {
    listen 443 ssl;
    server_name v-more.ru;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/v-more.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/v-more.ru/privkey.pem;

    # Trust Beget proxy headers
    set_real_ip_from 5.101.152.0/24;
    set_real_ip_from 5.101.152.161;
    real_ip_header X-Forwarded-For;
    real_ip_recursive on;

    # Security headers
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Main application
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }

    # API routes
    location /api/ {
        limit_req zone=api_zone burst=20 nodelay;
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # n8n webhooks
    location /webhook/ {
        proxy_pass http://127.0.0.1:5678;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # n8n UI (admin interface)
    location /n8n/ {
        proxy_pass http://127.0.0.1:5678/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }

    # Static images
    location /images/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Health check
    location /health {
        access_log off;
        return 200 "OK";
        add_header Content-Type text/plain;
    }
}

# n8n subdomain (n8n.v-more.ru)

# HTTP → HTTPS redirect for n8n.v-more.ru
server {
    listen 80;
    server_name n8n.v-more.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    return 301 https://$host$request_uri;
}

# HTTPS server for n8n.v-more.ru
server {
    listen 443 ssl;
    server_name n8n.v-more.ru;

    # SSL certificates (same as v-more.ru if you расширите сертификат через certbot --expand)
    ssl_certificate /etc/letsencrypt/live/v-more.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/v-more.ru/privkey.pem;

    # Proxy all requests to n8n container
    location / {
        proxy_pass http://127.0.0.1:5678;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 86400;
    }
}
