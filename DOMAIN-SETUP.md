# 🌐 Настройка домена v-more.ru

Инструкция по настройке домена `v-more.ru` для проекта Boats2026.

## 📋 Шаги настройки

### 1. Настройка DNS

В панели управления вашего домена `v-more.ru` настройте DNS записи:

```
A запись:
@     → 193.42.127.120
www   → 193.42.127.120
```

**Важно:** После настройки DNS может потребоваться до 24 часов для распространения изменений (обычно 1-2 часа).

### 2. Обновление .env файла на сервере

Подключитесь к серверу и отредактируйте `.env` файл:

```bash
ssh root@193.42.127.120
cd /opt/boats2026
nano .env
```

Обновите следующие переменные:

```env
# Замените старый домен на v-more.ru
N8N_HOST=v-more.ru
N8N_WEBHOOK_URL=https://v-more.ru/webhook
NUXT_PUBLIC_APP_URL=https://v-more.ru
```

### 3. Обновление конфигурации Nginx

Конфигурация Nginx уже обновлена в репозитории. На сервере выполните:

```bash
cd /opt/boats2026
git pull
sudo cp nginx/system-nginx.conf /etc/nginx/sites-available/boats2026
sudo nginx -t
sudo systemctl reload nginx
```

Или используйте скрипт:

```bash
cd /opt/boats2026
git pull
sudo ./scripts/setup-system-nginx.sh
```

### 4. Перезапуск контейнеров

После обновления `.env` перезапустите контейнеры:

```bash
cd /opt/boats2026
docker compose restart
```

Особенно важно перезапустить контейнеры `app` и `n8n`, чтобы они подхватили новые переменные окружения.

### 5. Настройка SSL (HTTPS)

Если у вас есть SSL сертификат, настройте его в Nginx. Для автоматического получения бесплатного сертификата используйте Let's Encrypt:

```bash
# Установка Certbot
sudo apt-get update
sudo apt-get install -y certbot python3-certbot-nginx

# Получение сертификата
sudo certbot --nginx -d v-more.ru -d www.v-more.ru
```

Certbot автоматически обновит конфигурацию Nginx для HTTPS.

### 6. Проверка

После выполнения всех шагов проверьте:

```bash
# Проверка DNS
nslookup v-more.ru
nslookup www.v-more.ru

# Проверка доступности
curl -I http://v-more.ru
curl -I https://v-more.ru  # если настроен SSL

# Проверка контейнеров
docker compose ps

# Проверка логов
docker compose logs -f app
```

## 🔍 Проверка работы

1. Откройте в браузере: `http://v-more.ru` (или `https://v-more.ru` если настроен SSL)
2. Проверьте API: `http://v-more.ru/api/health`
3. Проверьте админ-панель: `http://v-more.ru/admin`

## ⚠️ Важные замечания

1. **DNS распространение:** После настройки DNS может потребоваться время (обычно 1-2 часа, максимум 24 часа)

2. **Telegram Webhook:** Если используете Telegram бота, после смены домена нужно обновить webhook:
   ```bash
   # На сервере или локально
   curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
     -d "url=https://v-more.ru/api/telegram/webhook" \
     -d "secret_token=<YOUR_WEBHOOK_SECRET>"
   ```

3. **n8n Webhooks:** Если используете n8n, проверьте, что все webhooks используют новый домен `v-more.ru`

4. **Кэш браузера:** После настройки домена очистите кэш браузера или используйте режим инкогнито

## 🐛 Решение проблем

### Домен не открывается

```bash
# Проверьте DNS
nslookup v-more.ru

# Проверьте Nginx
sudo nginx -t
sudo systemctl status nginx

# Проверьте контейнеры
docker compose ps
docker compose logs app
```

### Ошибка 502 Bad Gateway

```bash
# Проверьте, что приложение запущено
docker compose ps app

# Проверьте логи
docker compose logs app

# Перезапустите приложение
docker compose restart app
```

### SSL не работает

```bash
# Проверьте конфигурацию SSL
sudo nginx -t
sudo cat /etc/nginx/sites-available/boats2026 | grep ssl

# Перезапустите Nginx
sudo systemctl reload nginx
```

## 📝 Структура конфигурации

- `nginx/system-nginx.conf` - конфигурация Nginx для домена
- `.env` - переменные окружения (на сервере)
- `docker-compose.yml` - конфигурация Docker контейнеров

## ✅ Чеклист

- [ ] DNS записи настроены (A запись для @ и www)
- [ ] `.env` файл обновлен на сервере
- [ ] Конфигурация Nginx обновлена
- [ ] Контейнеры перезапущены
- [ ] SSL сертификат настроен (опционально)
- [ ] Telegram webhook обновлен (если используется)
- [ ] Сайт открывается по новому домену
- [ ] API работает корректно
