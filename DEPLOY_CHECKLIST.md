# ✅ Чеклист деплоя Boats2026

Используйте этот чеклист для проверки готовности к деплою.

## 📋 Предварительная подготовка

### 1. Переменные окружения
- [ ] Создан файл `.env` на основе `.env.example`
- [ ] Заполнен `DATABASE_URL` (PostgreSQL)
- [ ] Заполнен `TELEGRAM_BOT_TOKEN`
- [ ] Заполнен `TELEGRAM_BOT_USERNAME`
- [ ] Заполнен `TELEGRAM_WEBHOOK_SECRET` (случайная строка)
- [ ] Заполнен `N8N_WEBHOOK_URL` (если используется n8n)
- [ ] Заполнен `N8N_API_KEY` (если используется n8n)
- [ ] Заполнен `APP_URL` (URL вашего домена)

### 2. Docker
- [ ] Docker установлен и запущен
- [ ] Docker Compose установлен
- [ ] Проверено: `docker --version` и `docker-compose --version`

### 3. Домен и SSL (для продакшн)
- [ ] Домен настроен и указывает на IP сервера
- [ ] SSL сертификаты получены (Let's Encrypt)
- [ ] Сертификаты скопированы в `nginx/ssl/`
- [ ] Nginx конфигурация обновлена (`production.conf.example` → `default.conf`)

## 🚀 Деплой

### Вариант A: Автоматический (рекомендуется)

**Linux/Mac:**
- [ ] `chmod +x scripts/deploy.sh`
- [ ] `./scripts/deploy.sh`

**Windows:**
- [ ] `.\scripts\deploy.ps1`

### Вариант B: Ручной

- [ ] `docker-compose up -d --build`
- [ ] `docker-compose exec app npm run db:push`
- [ ] `docker-compose exec app npm run db:seed` (опционально)

## ✅ Проверка после деплоя

### 1. Контейнеры
- [ ] Все контейнеры запущены: `docker-compose ps`
- [ ] Нет ошибок в логах: `docker-compose logs`

### 2. Приложение
- [ ] Сайт открывается: http://localhost (или ваш домен)
- [ ] Health check работает: http://localhost/health
- [ ] API отвечает: http://localhost/api/health

### 3. База данных
- [ ] БД инициализирована: `docker-compose exec app npm run db:push`
- [ ] Данные загружены (если использовали seed)

### 4. Telegram Bot
- [ ] Webhook установлен: 
  ```bash
  curl -X POST "https://api.telegram.org/bot<TOKEN>/setWebhook" \
    -d "url=https://your-domain.com/api/telegram/webhook" \
    -d "secret_token=<SECRET>"
  ```
- [ ] Webhook проверен:
  ```bash
  curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
  ```
- [ ] Бот отвечает на команды в Telegram

### 5. n8n (если используется)
- [ ] n8n доступен: http://localhost:5678
- [ ] Авторизация работает
- [ ] Webhooks настроены

## 🔒 Безопасность

- [ ] Firewall настроен (только 22, 80, 443 открыты)
- [ ] SSL сертификаты валидны
- [ ] Пароли в `.env` сильные
- [ ] `.env` файл не в git (проверьте `.gitignore`)
- [ ] n8n защищен паролем (если доступен извне)

## 📊 Мониторинг

- [ ] Логи проверены: `docker-compose logs -f`
- [ ] Ресурсы проверены: `docker stats`
- [ ] Health checks работают

## 🔄 Обновление

После изменений в коде:

- [ ] `git pull` (получить обновления)
- [ ] `docker-compose up -d --build` (пересобрать)
- [ ] `docker-compose exec app npm run db:push` (миграции, если есть)
- [ ] Проверка работоспособности

## 🆘 Если что-то пошло не так

1. **Проверьте логи:**
   ```bash
   docker-compose logs app
   docker-compose logs nginx
   docker-compose logs postgres
   ```

2. **Проверьте переменные окружения:**
   ```bash
   docker-compose exec app env | grep -E "DATABASE|TELEGRAM|N8N"
   ```

3. **Перезапустите контейнеры:**
   ```bash
   docker-compose restart
   ```

4. **Полный перезапуск:**
   ```bash
   docker-compose down
   docker-compose up -d --build
   ```

## 📚 Дополнительная документация

- [QUICK_DEPLOY.md](./QUICK_DEPLOY.md) - быстрая инструкция
- [docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md) - полная документация
- [docs/TELEGRAM_SETUP.md](./docs/TELEGRAM_SETUP.md) - настройка Telegram
- [docs/N8N_SETUP.md](./docs/N8N_SETUP.md) - настройка n8n

---

**Готово!** ✅ Если все пункты отмечены, ваше приложение готово к работе.
