# 🧪 Тестовый деплой без настройки .env

Быстрый способ запустить приложение для тестирования без настройки переменных окружения.

## Быстрый старт

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/test-deploy.sh

# Запустите тестовый деплой
./scripts/test-deploy.sh
```

Скрипт автоматически:
- ✅ Создаст минимальный `.env` с дефолтными значениями
- ✅ Запустит все контейнеры (app, postgres, nginx, n8n)
- ✅ Инициализирует базу данных
- ✅ Покажет статус и полезные команды

## Что будет использовано по умолчанию

- **База данных:** PostgreSQL с дефолтными креденшелами
  - User: `boats`
  - Password: `boats2026secret`
  - Database: `boats2026`

- **Telegram:** Заглушки (бот не будет работать)
  - Token: `test-token`
  - Username: `test-bot`

- **n8n:** Доступен на порту 5678
  - User: `admin`
  - Password: `admin2026`

## Доступ к приложению

После запуска приложение будет доступно:

- **Основное приложение:** http://localhost
- **Напрямую (порт 3000):** http://localhost:3000
- **n8n панель:** http://localhost:5678
- **Health check:** http://localhost/health

## Проверка работоспособности

```bash
# Проверка статуса контейнеров
docker-compose ps

# Проверка health check
curl http://localhost/health
curl http://localhost/api/health

# Просмотр логов
docker-compose logs -f app
```

## Ручной запуск (без скрипта)

Если хотите запустить вручную:

```bash
# 1. Создайте минимальный .env (если его нет)
cat > .env << 'EOF'
DATABASE_URL="postgresql://boats:boats2026secret@postgres:5432/boats2026?schema=public"
TELEGRAM_BOT_TOKEN=test-token
TELEGRAM_BOT_USERNAME=test-bot
TELEGRAM_WEBHOOK_SECRET=test-secret
N8N_WEBHOOK_URL=http://n8n:5678/webhook
NODE_ENV=production
APP_URL=http://localhost
POSTGRES_USER=boats
POSTGRES_PASSWORD=boats2026secret
POSTGRES_DB=boats2026
N8N_USER=admin
N8N_PASSWORD=admin2026
N8N_HOST=localhost
EOF

# 2. Запустите контейнеры
docker-compose up -d --build

# 3. Подождите запуска БД
sleep 15

# 4. Инициализируйте БД
docker-compose exec app npm run db:push
```

## Что НЕ будет работать в тестовом режиме

- ❌ Telegram бот (нужен реальный токен)
- ❌ Webhook'и Telegram (нужен реальный домен)
- ❌ n8n интеграции (если требуют реальные API ключи)

## Что БУДЕТ работать

- ✅ Веб-интерфейс приложения
- ✅ Просмотр яхт и услуг
- ✅ Формы бронирования (но отправка может не работать)
- ✅ База данных PostgreSQL
- ✅ API endpoints
- ✅ n8n панель управления

## Остановка тестового окружения

```bash
# Остановка контейнеров
docker-compose down

# Остановка с удалением volumes (⚠️ удалит данные БД)
docker-compose down -v
```

## Переход к продакшн

Когда будете готовы к продакшн деплою:

1. Удалите тестовый `.env`:
   ```bash
   rm .env
   ```

2. Создайте правильный `.env`:
   ```bash
   cp .env.example .env
   nano .env  # Заполните реальными значениями
   ```

3. Запустите продакшн деплой:
   ```bash
   ./scripts/deploy.sh
   ```

## Troubleshooting

### Ошибка: порт 80 занят

```bash
# Проверьте, что использует порт
sudo netstat -tulpn | grep :80

# Остановите nginx на хосте (если есть)
sudo systemctl stop nginx
```

### Ошибка: недостаточно места

```bash
# Очистите неиспользуемые Docker образы
docker system prune -a
```

### Ошибка: база данных не запускается

```bash
# Проверьте логи PostgreSQL
docker-compose logs postgres

# Пересоздайте контейнер БД
docker-compose down
docker-compose up -d postgres
sleep 10
docker-compose up -d
```

### Приложение не отвечает

```bash
# Проверьте логи приложения
docker-compose logs app

# Проверьте, что контейнер запущен
docker-compose ps

# Перезапустите приложение
docker-compose restart app
```

---

**Готово!** 🎉 Теперь вы можете протестировать приложение без настройки всех переменных окружения.
