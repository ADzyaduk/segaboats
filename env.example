# Boats2026 — пример переменных окружения
# На сервере: скопируйте в .env и подставьте свои значения.
#   cp env.example .env
#   nano .env
# Файл .env в git не коммитить (уже в .gitignore).

# --- База данных ---
POSTGRES_USER=boats
POSTGRES_PASSWORD=смените_на_сложный_пароль
POSTGRES_DB=boats2026

# --- Админка сайта ---
ADMIN_PASSWORD=смените_на_сложный_пароль

# --- Telegram ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
TELEGRAM_ADMIN_CHAT_ID=
TELEGRAM_WEBHOOK_SECRET=

# --- n8n ---
N8N_USER=admin
N8N_PASSWORD=смените_на_сложный_пароль
N8N_HOST=n8n.v-more.ru
# Опционально: API-ключ n8n для скриптов
# N8N_API_KEY=

# Прокси только для исходящих запросов n8n (например к Google). Оставьте пустым, если не нужен.
# Формат: http://логин:пароль@хост:порт
# N8N_HTTP_PROXY=

# --- OpenRouter (AI) — опционально ---
# OPENROUTER_API_KEY=
